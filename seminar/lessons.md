# Секретный спецсеминер по специальности "Нумерашка" для студентов математических вузов

Одобрено мировой финансовой закулисой, спасено из костра, возжённого на Совете старейшин 16 кислева 5786. Состоит из Трёх Глав и Питоновой Сутры, которую читают с конца. 

"Единственное место, где можно легально и открыто показывать навыки — публичные турниры, ибо всё остальное мрак. Гигиена TLP-протокола (Traffic Light Protocol): по любому датасету, сигналу или репозиторию всегда задавай себе два вопроса: «Почему я это вижу? Могу ли я это видеть?». Прочти и повтори это вслух. Если ответ не «TLP:WHITE» или явное письменное разрешение — молчи, не копируй, не форварди, не скринь. Один неверный клик — и ты в чёрном списке навсегда, also, всегда соблюдай законодательство и контрактные обязательства. Блюди принципы кибернетической безопасности". (Чат-Сутра 0.1).

"Сегодня в кванте/альго-трейдинге конфиденциальность стала таким же жёстким фильтром, как и сам профит. Если ты не прошёл его на 100 %, тебя просто не пустят дальше ресепшена в любой серьёзный фонд или проп-шоп. Настоящие модели показывай только после подписанного офера и mNDA" (Пенсия-Упанишада 2.40)

"Сейчас мы прост пилим модели, а через пять лет будем пилить совсем другие весТчи, и мы будем рулить этой индустрией по-нашему." (Надпись в сортире одного из аэропортов)

"Я спросил у Чатушки, я спросил у Грока, что в моделе хорошо, а что в моделе плохо. Я Дипсику рассказал, почему не катит, а затём всё удалил — показал и хватит" (Вайбкодер Маньяковски, из поэмы "Облако в Штатах")

# Почему нужно работать в Linux

Пару вводных слов об обустройстве твоей лаборатории. Кванту вполне хватает умеренно старого геймерского ноутбука с дискретной видеокартой, а больной счёт всё равно выполняется на кластерах. Помни, что "математика есть искусство избегать вычислений". Твои решения должны быть экономными. Здоровые ограничения заставят тебя думать об алгоритмах, инфраструктуре и оптимизации.

1. **Linux = стандартная среда для DevOps и квантов**

   * Работа на собственном ноутбуке в Linux — это как учить иностранный язык по методу Лорда Байрона.
   * Рано или поздно любой квант сталкивается с серверами, облачными машинами и кластерными вычислениями.
   * Linux — это юниксоид и **де-факто стандарт** на серверах для работы с большими данными и ML (заметим, что FreeBSD для квантов не очень типично).
   * Если ты ещё не, то на рабочий ноутбук ставь Linux Mint Mate, субъективно один из самых уютных дистрибутивов.
   * Единственное исключение — работа на Маке, потому что MacOS сам по себе не просто юниксоид, а единственная массовая десктопная ОС, которая является сертифицированным Unix.
   * VPS на Debian и ноутбук на Mint — это прям дзен. Всё знакомо, всё работает одинаково, меньше сюрпризов и когнитивной нагрузки (команды те же самые, пути тоже).
   * VPS-ка, даже самая ничтожная, это как если иметь маленькую-маленькую квартирку в Париже, бывает весьма удобно, но для начала можно и обойтись. 

2. **Командная строка ускоряет работу с большими файлами**

   * Когда файлы на сотни мегабайт или гигабайт, Excel и GUI-инструменты не справятся.
   * В Linux можно мгновенно посчитать строки, найти уникальные значения, фильтровать или объединять файлы с помощью команд:

```bash
wc -l big_file.csv        # количество строк
head -n 10 big_file.csv   # первые 10 строк
grep "BTC" big_file.csv   # фильтрация по символу
sort data.csv | uniq      # уникальные значения
```

3. **Парсинг и подготовка данных**

   * Большинство скриптов для скачивания и обработки данных написаны для Linux/Unix.
   * Знание команд `awk`, `sed`, `cut`, `grep`, `find`, `xargs` позволяет быстро чистить, трансформировать и агрегировать данные.

4. **Автоматизация и DevOps-навыки**

   * Работа с Linux учит запускать скрипты, планировать задачи (`cron`), управлять ресурсами.
   * Это очень полезно для квантов и ML-инженеров, которые хотят работать с **кластерами, облаком и пайплайнами данных**.

> Работать с Linux — это как учить язык страны, где-таки собираемся жить. На ноутбуке можно практиковаться, а на сервере это просто необходимо.

---

# 📌 Занятие 1

# Вводные замечания: ML-подход к торговым данным

Машинное обучение в торговле (ML в финтеке) есть практика разработки, тестирования и применения моделей, которые делают прогнозы на основе анализа исторических и альтернативных данных.

* **ML-модель (или ML-бот)** **не обязательно исполняет сделки сама**. Она генерирует предсказания о будущей доходности, направлениях движения рынка или сигналах для принятия решений.
* **Отличие от классических торговых роботов:**
    * Классические роботы часто используют один конкретный алгоритм или стратегию (“эксплойт”), например, арбитраж на одной бирже.
    * ML-модели **обучаются на данных**, могут учитывать сотни или тысячи признаков, адаптироваться к изменяющемуся рынку и обнаруживать паттерны, которые сложно описать вручную.

**Ключевая идея:**

ML-модель делает предсказания на основе данных, а не на основе заранее запрограммированного “приёма”. Она выявляет (ага, как бы сама) статистические закономерности, которые могут давать альфу — устойчивое преимущество над рынком.

# Почему одной цены недостаточно?

1. **Цена — это просто уровень актива в момент времени.**
    * Если модель будет пытаться “предсказывать цену” напрямую, она почти всегда будет проигрывать простому наивному предположению: “цена завтра ≈ цена сегодня”.
    * Почему? Цены финансовых активов обладают свойством **случайного блуждания** (Random Walk), особенно на высокочастотных тайм-сериях.
    * Следствие: прямая предсказательная модель цены (в лоб) не даёт устойчивого преимущества.
2. **Говно положишь — говно получишь**
    * Если использовать только базовые данные, например только цену закрытия, большинство участников рынка получают одни и те же входные данные и модель не будет выдавать конкурентнога преимущества.
    * Чтобы извлечь **альфу**, нужно иметь информацию, которая предсказывает будущую доходность и неизвестна другим игрокам.

***

# Что такое альфа?

**Альфа (α)** — это **превышение доходности над рыночной нормой или бенчмарком**.

* Для **трейдера**: возможность получить прибыль выше среднего рынка.
* Для **инвестора**: разница между доходностью портфеля и индексом рынка при аналогичном риске.
* Для **исследователя данных / Quant**: способность построить модель, которая предсказывает **отклонения доходности от случайного или “базового” поведения рынка**.

Альфа не существует без уникальной информации. Доступ к “обычным” данным (цена, объем) почти всегда уже встроен в рынок, т.е. альфа = 0.

***

# Данные для построения моделей

1. **Обычные данные**:
    * Исторические цены (OHLCV)
    * Технические индикаторы (SMA, EMA, RSI, MACD и т.д.), которыми пользуются трейдеры
    * Эти данные используют почти все, они не представляют особого интереса
2. **Эксклюзивные и редкие данные**:
    * Альтернативные источники (social sentiment, on-chain данные криптовалют, метрики поставок, объемы заказов, внутренние отчеты, геополитика, данные об изменениях в портфеле крупного инвестора и т.д.).
    * На рынке такие данные стоят дорого. Они могут содержать **информацию, которую другие участники не используют**, то есть потенциальную альфу.

***

# 📌 Занятие 2. Что предсказывают модели?

Важно понять:

* Модели не предсказывают цену напрямую!
* Модели обычно предсказывают возврат (return) или сигнал направленности:

$$
r_{t+1} = \frac{P_{t+1} - P_t}{P_t}
$$


или сигнал (signal), который указывает, **покупать или продавать**.

* В конкурсах вроде Numerai участники предстказыют таргет (target).
    * Таргет создаётся организаторами (обычно на основе будущей доходности), без объяснений о том, что он из себя представлят конкретно.
    * Участник строит **фичи** → обучает модель → создает сигнал для каждого актива, с помощью которого пытается предсказать таргет
    * В недостижимом идеале сигнал = таргету
    * Цель участников максимизировать **корреляцию предсказаний с будущей доходностью**, а не угадывать цену.

***

# Разработка таргета (target)

* **Что такое target?**: числовая величина, отражающая будущую доходность или ее ранговую позицию.
* В Numerai target уже “смещён” на будущее (forward-looking), чтобы участники не использовали будущие данные напрямую.
* **Разработка собственного target**:
    * Выбираем горизонт прогнозирования (1 день, 7 дней, месяц)
    * Вычисляем будущую доходность:

$$
r_{t+h} = \frac{P_{t+h} - P_t}{P_t}
$$


$r_{t+h}$ — доходность на горизонте h

$P_t$ — цена в момент t

$P_{t+h}$ — цена через h периодов

    * Можно ранжировать или нормировать по символам для стабильности обучения

**Важная идея:** target = то, что модель должна предсказать, а не цена как таковая.

***

# Краткая формула Data Science подхода к альфе

Альфа=f(редкие данные, уникальные фичи)  ⟹  предсказание будущей доходности  ⟹  торговая стратегия\\text{Альфа} = f(\\text{редкие данные, уникальные фичи}) \\implies \\text{предсказание будущей доходности} \\implies \\text{торговая стратегия}Альфа=f(редкие данные, уникальные фичи)⟹предсказание будущей доходности⟹торговая стратегия

***

# Итоги

1. Цены сами по себе малоинформативны → “предсказание цены” почти всегда бесполезно.
2. Фокус на **return / сигнал**, а не на цену.
3. Альфа появляется из **уникальных данных и хорошо построенных признаков**.
4. Target = будущее поведение рынка (return, сигнал), а не сама цена.
5. Валидация по времени и защита от утечек = ключ к честной оценке модели.

# 📌 Занятие 3

# **Что такое “сдвиг данных” (shifting) в тайм-сериях?**

Представь, что мы хотим научить модель предсказывать **завтрашнюю доходность** акции. У нас есть данные по дням:

| День | Цена |
| ---- | ---- |
| t | 100 |
| t+1 | 101 |
| t+2 | 99 |

Чтобы модель училась правильно, ей нужно давать:

* **фичи (признаки) за сегодня**
* **цель = доходность за завтра**

Но изначально доходность считается так:

```
ret[t] = (price[t] – price[t−1]) / price[t−1]
```

Это доходность **прошлого дня**, а нам нужна **будущего**. Поэтому мы делаем **сдвиг**.

***

# Что такое доходность (return)?

**Доходность** — это насколько изменилась цена:

```
return = (новая цена – старая цена) / старая цена
```

Если цена выросла с 100 до 101:

```
ret = (101 – 100)/100 = 0.01 (1%)
```

***

# Что такое *forward-looking* return?

Это **доходность будущего дня**, то есть: «Сколько цена изменится завтра относительно сегодня?». Чтобы получить такую доходность, мы берём обычную доходность и **сдвигаем её вверх**:

```
df['target'] = df['ret'].shift(-1)
```

***

# Простое объяснение сдвига

Вот строки данных:

| День | Цена | ret (прошлая) | target (будущая) |
| ---- | ---- | ------------- | ---------------- |
| t | 100 | — | **ret в t+1** |
| t+1 | 101 | 0.01 | **ret в t+2** |
| t+2 | 99 | -0.019 | — |

То есть мы берём:

* фичи за **t**
* цель = доходность за **t+1**

***

**Shifting — это сдвиг данных так, чтобы признаки были “сегодня”, а цель — “завтра”. Return — это, насколько изменилась цена. Forward-looking return — это завтрашний return, который нужно предсказывать, поэтому мы сдвигаем его вверх по таблице.**
Вот **супер коротко, на пальцах**, с примером Python:

```
# считаем дневную доходность
df['ret'] = df['close'].pct_change()

# сдвигаем на 1 день вперед, чтобы предсказывать будущую доходность
df['target'] = df['ret'].shift(-1)
```

* `ret` = прошлый рост цены
* `target` = будущий рост цены (то, что хотим предсказать)

# Что мы сделали сдвигом

* Мы **сдвинули целевую переменную (`target`) вверх** на 1 день (`shift(-1)`), чтобы каждая строка содержала:

```
фичи за t → target за t+1
```

* **Фичи мы не трогали**, они остались “сегодняшними”.
* Но с точки зрения обучения, теперь каждая строка **смотрит в будущее через target**.

То есть фактически:

| день t | фичи(t) | target(t+1) |
| ------ | ------- | ----------- |
|

***

# 2️⃣ Теперь запускаем классификатор

Да, после этого:

1. Отбираем признаки: `f1, f2, …, fn`
2. Берём выровненный target
3. Создаём матрицу признаков X и вектор целей y

```
X = df[['f1','f2','f3', ...]]
y = df['target']
```

4. Делим данные на **train/validation** (лучше по времени, чтобы не было утечки).
5. Обучаем классификатор или регрессор (например, XGBoost):

```
import xgboost as xgb

dtrain = xgb.DMatrix(X_train, label=y_train)
dvalid = xgb.DMatrix(X_valid, label=y_valid)

model = xgb.train(params, dtrain, num_boost_round=1000, evals=[(dvalid,'eval')])
```

***

# ✅ Ключевой момент

* **Фичи не сдвигаем**.
* **Target сдвинут** на +1 день (forward-looking).
* После этого можно **обучать модель** и делать предсказания на текущие фичи, чтобы предсказывать будущую доходность.

# 1️⃣ Что такое “утечка таргета” (target leakage)

**Target leakage** — это ситуация, когда **входные признаки (фичи) содержат информацию о будущем таргете**, которую модель не должна знать в момент предсказания.

* В результате модель **учится “читерить”**, и валидирующие метрики выглядят слишком хорошо.
* В реальной жизни такая модель **провалится на новых данных**, потому что будущая информация недоступна.

***

# 2️⃣ Фичи сильно коррелируют с таргетом — это всегда утечка?

**Нет, не всегда.**

* **Корреляция естественная**: если фича рассчитывается только на основе **прошлых данных**, а таргет — это будущая доходность, то высокая корреляция **может быть нормальной**.
    Пример: 5-дневная скользящая средняя (`SMA5`) vs будущая доходность.
    Модель просто “видит тренд” — это **законная информация**.
* **Утечка возникает**, если фича включает **данные из будущего**, которые на момент предсказания **не доступны**.
    Примеры утечки:
    * Использовать цену закрытия **завтрашнего дня** при вычислении индикатора.
    * Прямо брать target или его производные (например, “следующий день return”) как вход.

***

# 3️⃣ Как проверить, есть ли утечка

1. Смотрим на фичи и target. Если фича использует **t+1, t+2 …**, это утечка.
2. Корреляция сама по себе **не показатель утечки**.
3. Простой тест:
    * Отрезаем все фичи до **t**
    * Обучаем модель
    * Если точность падает резко, возможно была утечка.

***

# 4️⃣ Правило для тайм-серий

* Фичи могут быть **сильно коррелированы с будущим таргетом**, если они построены только на **прошлой или текущей информации**, то это нормально.
* **Нельзя** включать в модель **любую будущую информацию**, даже косвенно.

# **Правило гигиены данных (Data Hygiene Rule)**

**Ни одна фича, используемая для обучения модели, не должна содержать информации о будущем относительно момента предсказания.**

***

# **Что это значит на практике**

1. **Используй только прошлое и настоящее** для вычисления признаков:
    * Скользящие средние, волатильность, индикаторы — только на основе цен до текущего дня.
    * Лаговые признаки (`feature.shift(1)`, `feature.shift(5)`) безопасны.
2. **Не включай будущие цены или таргет**:
    * Прямое использование `target` в качестве фичи = утечка.
    * Любые признаки, которые используют t+1, t+2, … = утечка.
3. **Проверяй фичи на корреляцию с таргетом**, но помни: высокая корреляция не всегда утечка; важно, откуда берётся информация.
4. **Разделяй данные по времени для обучения/валидации**:
    * Валидируй на “будущих” данных относительно обучающих.
    * Никогда не перемешивай эпохи/даты при сплите (chronological split).
5. **Автоматическая проверка утечки**:
    * Попробуй смоделировать таргет на случайных фичах будущего; если метрики слишком высокие → есть утечка.


---

# Малая Питоновая Сутра

Слишком много букв? Конечно, дружок! Давай перепишем конспект в стиле **Python + комментарии**, чтобы ты сразу просёк формулы и концепцию в коде.

# 📌 Почему одной цены недостаточно

```python
# Цены сами по себе малоинформативны
# Наивная стратегия: "цена завтра ≈ цена сегодня"
price_today = 100
price_tomorrow_pred = price_today  # почти всегда лучше простого предсказания сложно
```

---

# 📌 Что такое доходность (return)

```python
# Однодневная доходность
ret_1d = (price_tomorrow - price_today) / price_today
# в Pandas
df['ret_1d'] = df['close'].pct_change()
```

---

# 📌 Forward-looking return (будущая доходность)

```python
# Target = доходность на следующий день
df['target'] = df['ret_1d'].shift(-1)
# теперь для строки t:
# признаки = данные на t
# target = доходность t+1
```

---

# 📌 Альфа (α)

```python
# Альфа = доходность сверх базового рынка или индекса
alpha = strategy_return - market_return
# Для трейдера: α = "прибыль выше рынка"
# Для исследователя данных: α = предсказуемая часть будущей доходности
```

---

# 📌 Признаки (features)

```python
# Используем только прошлые и текущие данные
df['SMA5'] = df['close'].rolling(5).mean()   # скользящая средняя
df['volatility20'] = df['ret_1d'].rolling(20).std()  # волатильность

# НЕ использовать будущие цены
# df['SMA_future'] = df['close'].shift(-1).rolling(5).mean()  # Утечка таргета!
```

---

# 📌 Разработка таргета

```python
# DIY target: будущая доходность на h дней
h = 1  # горизонт предсказания
df['target'] = (df['close'].shift(-h) - df['close']) / df['close']

# Можно ранжировать для устойчивости
df['target_rank'] = df.groupby('symbol')['target'].rank(pct=True)
```

---

# 📌 Стратегия построения модели

```python
# X = признаки
features = ['SMA5', 'volatility20', ...]
X = df[features]

# y = таргет
y = df['target']

# Разделяем на train/validation по времени
train_size = int(len(df) * 0.7)
X_train, X_valid = X.iloc[:train_size], X.iloc[train_size:]
y_train, y_valid = y.iloc[:train_size], y.iloc[train_size:]

# Обучаем регрессор (например XGBoost)
import xgboost as xgb
dtrain = xgb.DMatrix(X_train, label=y_train)
dvalid = xgb.DMatrix(X_valid, label=y_valid)
model = xgb.train(params, dtrain, num_boost_round=1000, evals=[(dvalid,'eval')])
```

---

# 📌 Правило гигиены данных

```python
# Ни одна фича не должна использовать будущую информацию
# Фичи: только прошлое и текущее
# Target: будущее
# Сдвигаем target вперед
df['target'] = df['ret_1d'].shift(-1)
```

---

# 📌 Альфа и эксклюзивные данные

```python
# Базовые данные (цена, объем) почти все используют
# Уникальные данные = потенциальная альфа
# Например: социальные сети, цепочки блоков, редкие фундаментальные данные
alpha = model_prediction - baseline_return
```

---

# 💡 Вывод для студентов, которые читают с конца

* Мы не предсказываем цену, а **future return / сигнал**.
* Альфа возникает из **уникальных данных и правильного таргета**.
* Защита от утечек и правильный temporal split = ключ к честной оценке модели.


