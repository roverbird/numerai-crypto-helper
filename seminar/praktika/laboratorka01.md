# Лабораторные работы к спецсеминеру по специальности "Нумерашка"

# Лабораторная работа к Занятию 2: Строим Таргет своими руками

Открой скрипт `price30f.py` и файл с данными к нему, `real_price_input_data.csv`

Этот скрипт конструирует таргет 30-дневной доходности. Скрипт преобразует сырые почасовые данные в **целевую переменную (target)** для модели — будущую доходность за 30 дней.

1. Пусть

 - $P_t$ — цена закрытия в день $t$  
   - $P_{t+30}$ — цена закрытия через 30 дней

2. Скрипт использует **сдвиг временного ряда**:

```python
price_at_close_date = price.shift(-30)
```

что соответствует:

$$
[
P_{t+30} \leftarrow \text{shift}(P_t, -30)
]
$$

3. Доходность за 30 дней определяется как:

$$
[
r_{t+30} = \frac{P_{t+30} - P_t}{P_t}
]
$$

4. Таким образом, формируется таргет:

$$
[
\text{target}(t) = r_{t+30}
]
$$

---

# Время очень важно

Теперь открой файл `real_price_input_data.csv`, лучше всего сделать сделать это юникс-командой head из командной строки. Внимательно посмотри на кусочек данных и на хэдеры.

Обычно временные метки в реальных данных приходят **не в человеческом формате**, а в виде **Unix-времени** (количество секунд с 1 января 1970 года), и это абсолютно нормально — **Pandas легко переводит Unix timestamp ↔ человеческое время**, поэтому бояться этого не нужно. Однако есть ключевое правило дисциплины со временем:

### 🔥 **Все системы должны использовать одно и то же время — всегда UTC.**

* Никаких локальных часовых поясов.
* Никаких “часовых смен”, “летних времён”, “Europe/Berlin”, “America/New_York”.
* Никаких локалей, зависящих от языка.
* **Сервер должен работать в UTC.**
* **Все временные метки внутри пайплайна — в UTC.**

Это критически важно, потому что когда твои данные вырастут до миллионов строк, любая ошибка во временной зоне разрушит последовательность данных и приведёт к неправильным таргетам, утечкам будущей информации и неверным фичам.

Кроме того, важно понимать:

### 🔍 Разные источники данных кодируют время по-разному

* биржевые “сырые” трейды почти всегда используют **Unix epoch (int64)**
* агрегированные ценовые ряды часто используют **ISO-8601 строки**
* некоторые источники используют **микросекунды**, другие — **миллисекунды**

Поэтому требуется строгая дисциплина обратотки временем. Время реально один из самых таких подводных камней. Поищи команду, как из строки проверить временную зону в Linux на своем лэтопе и сделай эту проверку прямо сейчас. На локальном ноутбуке можно не паниковать, просто убедись, что при парсинге данных используется единый формат (UTC и человеко-читаемый YYYY-MM-DD HH:MM:SS). На сервере — без вариантов: все скрипты, cron jobs и пайплайны должны работать в UTC. Все сдвиги, таргеты и агрегации будут корректными только при строгой дисциплине времени.

* всегда приводить всё в один формат (лучше ISO-8601 строка: `YYYY-MM-DD HH:MM:SS`)
* всегда интерпретировать время как UTC и использовать UTC
* всегда проверять, из какого именно формата приходит timestamp

---

Идём по файлу с настоящими данными.

# 📌 Какие данные обрабатывает скрипт?

Скрипт ожидает на вход **сырой почасовой ценовой ряд** для одного торгового актива (один и тот же тикер в каждой строке файла).
Формат входного CSV-файла:

```
timestamp,symbol,price
2024-01-01 13:00:00,NAME,111.27121217159628
2024-01-01 14:00:00,NAME,111.22011879680194
2024-01-01 15:00:00,NAME,110.91199456534623
2024-01-01 16:00:00,NAME,112.19539637715133
...
```

### ✔ Столбцы:

* **timestamp** — дата и время (почасовые котировки)
* **symbol** — тикер актива (один и тот же во всём файле)
* **price** — фактическая цена актива в этот момент времени

---

Возвращаемся к файлу со скриптом на пайтоне.

# 📌 Что делает скрипт с этими данными?

1. Конвертирует timestamp → datetime
2. Извлекает **дневную дату** (`date = timestamp.date`)
3. Группирует по дате и выбирает **последнюю цену дня**

$$
   [
   P_t = \text{last price of day } t
   ]
$$

4. Сдвигает цену на 30 дней вперёд:

   ```python
   price_at_close_date = price.shift(-30)
   ```

   что соответствует:

$$
   [
   P_{t+30}
   ]
$$

5. Строит таргет — **будущую доходность за 30 дней**:

$$
   [
   r_{t+30} = \frac{P_{t+30} - P_t}{P_t}
   ]
$$

---

# 📌 Почему формат CSV (и что бывает в реальных проектах)

Этот учебный пример использует **CSV**, потому что:

* CSV легко открыть, понять и обработать
* Подходит для демонстрации принципов target engineering
* Удобен для командной строки (Linux) и быстрой проверки

Однако в реальных квантовых и финтех-проектах данные обычно хранятся в:

* **Parquet** — колонно-ориентированный формат, быстрый и сжатый
* **Feather** — формат на основе Apache Arrow, сверхбыстрый для Python/R
* **HDF5**, **Zarr**, **msgpack**, **binary blobs** — если объёмы очень большие

Эти форматы используются, потому что позволяют хранить и обрабатывать **десятки гигабайт** данных гораздо быстрее, чем CSV.

---

Запусти скрипт из командной строки и получи результат. Вот короткое объяснение результата обработки:

Итак, результат — это таблица, где для **каждой исходной строки с ценой на дату `date`** мы находим:

* **`close_date`** — дату на *30 дней вперёд*
* **`price_at_close_date`** — фактическую цену в этот будущий день
* **`30_day_future_return`** — процентная доходность между `price` и будущей ценой

---

### Нахрена это нужно? 

1. **Скрипт превращает исторические данные в обучающие данные.**
   Теперь у каждой строки есть “будущее”, которое модель может предсказывать.

2. **Проверяет способность алгоритма предсказывать реальную доходность, а не просто движение цен.**

3. **Создаёт строгий временной порядок.**
   Никакого заглядывания в будущее: берём только то, что на самом деле произошло через 30 дней.

4. **Унифицирует данные.**
   У всех примеров одинаковый горизонт — 30 дней, что очень важно для качественного обучения.


### И что же мы видим в этих результатах?

```
symbol,date,price,close_date,price_at_close_date,30_day_future_return
NAME,2024-01-01,115.10,2024-01-31,86.27,-25.05%
NAME,2024-01-02,110.13,2024-02-01,83.70,-24.00%
NAME,2024-01-03,102.36,2024-02-02,85.44,-16.53%
```

* Каждая строка красноречиво говорит: «Если бы ты купил в этот день по цене X, то через 30 дней получил бы доходность Y».
* Но все доходности отрицательные ибо рынок в этот период падал, так что у держателя актива были беспокойства. 

> А почему именно 30 дней, товарищи? 30 дней выбирают потому, что это типичный горизонт удержания позиции в маркет-нейтральных и факторных хедж-фондах. Вообще, 30-дневный горизонт — это нормальная и широко распространённая практика среди институциональных инвесторов, потому что такой срок балансирует между устойчивостью сигналов и контролем транзакционных издержек при регулярной ребалансировке портфеля. И таки да, относительно таргета Numerai мы можем только гадать: Numerai не раскрывает точный горизонт, но по поведению метрик и структуре данных многие предполагают, что их таргет основан на неком среднем или сглаженном будущем горизонте (вроде 20–30 дней), однако формула остаётся закрытой. Для участников турнира это действительно может серьёзно “подвинуть мозги”, потому что сама идея скрытого горизонта и того, что модель на самом деле предсказывает не ближайшее движение, а усреднённый будущий период — совсем не очевидна и _ломает обывательские представления о торговле криптой или акциями_.

---
# Ура, таргет построен!

Да, дружок, **таргет построен — но именно этот таргет никому не нужен**, если только ты **не создаёшь свою собственную систему тестирования прогнозов** или учебный пайплайн.

Почему, товарищи, спрашивал в таких случаях И.В.Сталин. Потому что:

* **Это не тот таргет, который использует Numerai**, и не тот таргет, по которому оценивают твои предсказания.
* Этот таргет просто демонстрация принципа, как можно вручную построить будущую доходность на фиксированном горизонте.
* Он полезен **для обучения**, **для проверки модели на исторических данных**, **для понимания связи между ценами и прогнозами**.
* Но **он не является боевым**, не подходит для реальной отправки в турнир, и не будет совпадать с истинными метками из Numerai Crypto Signals или Main Tournament.

Так что таки да таргет есть, но практическую ценность он имеет только в твоей собственной исследовательской лаборатории, где ты тестируешь гипотезы и учишься мыслить как кванты. И всё же это реальный и понятный таргет, у него может быть применение.

# Глючные понятие в лабораторной работе

Если ты проделал всё, о чём мы говорили, у тебя в голове должно было возникнуть пару “глюков”.

1. Дневной close — не берём просто первый или средний, берём последнюю цену дня.

2. Сдвиг временного ряда (shift) — создаёт “будущее” для таргета, чтобы модель могла предсказывать доходность.

3. Доходность, оно же returns — найди формулу в скрипте и проверь правильность вычислений вручную в двух случайных местах

4. NaN на конце аутпута — естественное последствие shift, требует удаления, иначе модель увидит мусор.

# Нужно научиться мыть руки

Попытаемся сформулировать **общее практическое правило работы с данными** для учеников и начинающих квантов:

**Проверка вычисленных переменных — это обязательная гигиена.**

* **Spot check**: всегда вручную проверяй 1–2 строки output, подставляя входные значения в формулы, чтобы убедиться, что рэшный или питоновый код считает правильно.
* **Row count check**: сверяй количество строк во входном и выходном файле, учитывая, что после сдвига часть строк теряется.
* **Sense check**: оценивай результат глазами — доходности должны иметь ожидаемый знак и порядок величины относительно изменения цен.

Эта мини-рутина превращается в жесткую привычку экспериментатора, которая защищает от логических ошибок, утечки будущей информации и «мусора», особенно когда данные растут и становятся очень большими.

> ⚡ Лучшее правило: каждый output сверять с собственными ожиданиями, чтобы понять, что реально происходит, а не полагаться на одну только магию скрипта.

# Домашка для любознательной

Перепиши код на языке R, можешь использовать нейронку. Прогони через R и сравни результаты. Они, конечно, должны в точности совпасть. Подумай, как можно сравнить два файла из командной строки в Линуксе и попробуй это сделать.

## Твои скилы после выполнения домашней работы:

- Переписывание кода учит тебя видеть алгоритм отдельно от синтаксиса языка.
- Ты перестаёшь слепо доверять одному орудию, у тебя появляется возможность объективного контроля результатов и альтернативы в выборе лабораторного инструмента
- Сравнение файлов в коммандной строке учит дисциплине проверки результатов.

